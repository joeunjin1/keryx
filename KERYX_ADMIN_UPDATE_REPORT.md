# KERYX 관리자 페이지 업데이트 보고서

## 1. 업데이트 개요
KERYX의 핵심 가치인 "IP 제안과 장기적 사업플랜 제시"를 관리자 페이지에 반영하기 위해, 기존의 단순 거래 실행 도구에서 **"파트너 파이프라인 + IP 제안 운영 본부"**로 전면 개편했습니다.

## 2. 주요 변경 사항

### 2.1 사이드바 메뉴 재구성 및 용어 통일
- **용어 변경**: 기존의 '바이어'를 모두 **'파트너'**로 변경하여 장기적인 협력 관계를 강조했습니다.
- **메뉴 최상단 배치**: 
  - `파트너 관리` 그룹 신설 (파트너 파이프라인, 파트너 목록, 신규 문의)
  - `IP 제안·사업플랜` 그룹 신설 (IP 제안 보드, IP Studio, IP 라이센스 관리)
- **기존 기능 유지**: 공장 매칭, 상품 관리, 주문·거래 관리 등 기존의 잘 작동하는 기능은 하단에 그대로 유지했습니다.

### 2.2 대시보드 위젯 재배치 (`/admin`)
- **빠른 등록**: '바이어 등록'을 '파트너 등록'으로 변경하고 가장 첫 번째 버튼으로 배치했습니다.
- **플랫폼 현황**: '활성 바이어'를 '활성 파트너'로 변경하고, 클릭 시 파트너 파이프라인으로 이동하도록 연결했습니다.
- **업무 도구**: 'IP 제안 보드' 바로가기를 추가하여 IP 제안 업무에 빠르게 접근할 수 있도록 했습니다.
- **포털 바로가기**: '바이어 포털'을 '파트너 포털'로 명칭을 변경했습니다.

### 2.3 신규 페이지 개발
1. **파트너 파이프라인 칸반 보드 (`/admin/partner-pipeline`)**
   - 파트너의 전체 라이프사이클(신규 문의 → IP 제안 준비 → IP 제안 발송 → 사업플랜 협의 → 샘플 개발 → 생산 진행 → 검수·납품 → 장기 파트너)을 한눈에 볼 수 있는 칸반 보드를 구현했습니다.
   - 각 파트너 카드를 클릭하면 상세 정보(담당자, 담당 MD, 제안 IP, 예상 주문액, 다음 액션)를 확인하고 단계를 이동할 수 있습니다.
   - 잠재 파트너를 빠르게 추가할 수 있는 모달을 제공합니다.

2. **IP 제안 보드 (`/admin/ip-proposals`)**
   - 파트너에게 제안한 IP(뿌찌프랜즈, 덕클, 디노몬 등)와 진행 상태(준비중, 발송 완료, 검토중, 수락, 거절)를 관리하는 페이지입니다.
   - 총 제안 수, 진행중, 수락 건수, 전환율 등 핵심 KPI를 상단에 표시합니다.
   - IP별 제안 현황을 시각적으로 확인하고 필터링할 수 있습니다.
   - 새 IP 제안을 생성할 때 파트너, 제안 IP, 제안 상품, 메모를 입력할 수 있습니다.

### 2.4 데이터베이스 스키마 추가
새로운 기능을 지원하기 위해 Supabase에 다음 테이블을 추가하는 마이그레이션 SQL을 작성했습니다 (`supabase/migrations/20260802_partner_pipeline_ip_proposals.sql`):
- `partner_pipeline`: 파트너 라이프사이클 및 단계 관리
- `ip_proposals`: IP 제안 이력 및 상태 관리
- `business_plans`: 파트너별 장기 사업플랜 관리 (추후 개발 예정)
- 각 테이블에 대한 RLS(Row Level Security) 정책을 설정하여 관리자/MD는 전체 접근, 파트너(Seller)는 자신의 데이터만 읽을 수 있도록 보안을 강화했습니다.

## 3. 빌드 및 배포 상태
- 로컬 빌드 테스트(`npm run build`)를 성공적으로 통과했습니다. (새로 추가된 페이지 에러 없음)
- 변경 사항을 GitHub `main` 브랜치에 커밋 및 푸시 완료했습니다.
- Vercel을 통해 자동 배포가 진행 중입니다.

## 4. 향후 계획 (Next Steps)
기획안에 명시된 나머지 항목들은 다음 단계에서 진행할 수 있습니다:
- **P6**: 사업플랜 보드 페이지 신규 개발 (`/admin/business-plans`)
- **P7**: 파트너 상세 페이지 확장 (파이프라인, IP 제안 이력, 사업플랜 통합 표시)
- **P8**: MD 배정 관리 페이지 개발
