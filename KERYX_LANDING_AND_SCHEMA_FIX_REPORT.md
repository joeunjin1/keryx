# KERYX 플랫폼 스키마 불일치 해결 및 랜딩 페이지 구축 완료 보고서

**작성일:** 2026년 5월 11일
**작성자:** Manus AI

---

## 1. 작업 개요

본 작업은 KERYX 플랫폼에서 발생하던 **"상품 등록 후 쇼핑몰 노출 시 발생하는 스키마 불일치 오류"**를 근본적으로 해결하고, 외부 광고 유입을 위한 **"맞춤형 랜딩 페이지 2종(수납용품, 여행·캠핑) 및 샘플 요청 기능"**을 신규 구축하는 것을 목표로 진행되었습니다.

---

## 2. 주요 작업 내용 및 결과

### 2.1. 상품/공장 API 스키마 불일치 근본적 해결
기존 시스템은 프론트엔드 코드와 API가 실제 Supabase 데이터베이스 스키마와 일치하지 않는 컬럼명(예: `product_images`, `product_pricing_tiers`, `factories.name` 등)을 참조하여, 상품 등록 후 쇼핑몰 페이지에서 오류가 발생하거나 데이터가 정상적으로 노출되지 않는 문제가 있었습니다.

*   **해결 사항:**
    *   **상품 상세 API (`/api/public/products/[id]/route.ts`):** 실제 DB에 존재하는 컬럼(`image_urls`, `sell_price_cny`, `moq` 등)만 조회하도록 쿼리를 전면 수정했습니다.
    *   **상품 상세 페이지 (`/products/[id]/page.tsx`):** API 응답 구조 변경에 맞춰 프론트엔드 컴포넌트의 데이터 바인딩을 실제 스키마에 맞게 정규화했습니다.
    *   **공장 스토어 페이지 (`/shop/factory/[factoryId]/page.tsx`):** 존재하지 않는 컬럼 참조를 제거하고, 실제 DB 컬럼을 사용하도록 수정했습니다.
    *   **공용 상품 API (`/api/public/products/route.ts`):** 존재하지 않는 컬럼(`short_desc_ko`, `material_ko` 등) 및 조인 오류(`factories.verified`)를 제거하여 API 호출 실패 문제를 해결했습니다.

### 2.2. 광고 랜딩 페이지 2종 구축
외부 광고(Google, Meta 등)를 통해 유입된 고객을 타겟팅하기 위한 맞춤형 랜딩 페이지를 구축했습니다.

*   **구축된 페이지:**
    *   **수납용품 전문 랜딩 페이지 (`/landing/storage`):** 수납용품 관련 공장 및 상품 노출
    *   **여행·캠핑 소품 랜딩 페이지 (`/landing/travel`):** 여행/캠핑 관련 공장 및 상품 노출
*   **주요 기능:**
    *   **다국어 지원:** 한국어/중국어 언어 전환 기능 제공 (`LandingPageTemplate` 공통 컴포넌트 활용)
    *   **동적 데이터 연동:** 관리자 페이지에서 설정한 배너 문구 및 매칭 공장 목록을 실시간으로 불러와 표시
    *   **상품 노출:** 선택된 공장들의 최신 상품을 자동으로 불러와 갤러리 형태로 제공

### 2.3. 샘플 요청 및 문의 관리 시스템 구축
랜딩 페이지를 통해 유입된 고객이 관심 있는 상품에 대해 샘플을 요청하거나 일반 문의를 남길 수 있는 시스템을 구축했습니다.

*   **DB 테이블 신설:**
    *   `landing_pages`: 랜딩 페이지 기본 정보 및 설정(배너 문구, 매칭 공장 등) 저장
    *   `landing_page_inquiries`: 고객 문의 및 샘플 요청 내역 저장
*   **고객용 기능:** 랜딩 페이지 하단에 문의 폼 제공 (일반 문의, 샘플 요청, 견적 요청 선택 가능)
*   **관리자/MD용 기능:**
    *   **문의 관리 페이지 (`/admin/landing`):** 접수된 문의 목록 확인, 상태 변경(대기중, 처리중, 답변완료, 종료), 답변 작성 기능 제공
    *   **페이지 설정 관리 (`/admin/landing/settings`):** 랜딩 페이지별 활성화 여부, 배너 문구 수정, 노출할 공장 선택 기능 제공
    *   **사이드바 연동:** 관리자 및 MD 사이드바 메뉴에 '랜딩 페이지' 그룹 추가

### 2.4. 빌드 및 배포
*   TypeScript 타입 검사 및 Next.js 빌드 테스트를 성공적으로 통과했습니다.
*   모든 변경 사항을 GitHub `main` 브랜치에 커밋 및 푸시 완료하여 Vercel 자동 배포 파이프라인을 트리거했습니다.

---

## 3. 향후 권장 사항

1.  **데이터베이스 스키마 관리:** 향후 DB 스키마 변경 시, 프론트엔드 코드 및 API 쿼리도 반드시 함께 업데이트하여 불일치 오류를 예방해야 합니다.
2.  **랜딩 페이지 성과 분석:** 랜딩 페이지별 조회수(`view_count`) 및 문의수(`inquiry_count`) 데이터를 활용하여 광고 캠페인의 성과를 분석하고 페이지를 최적화하는 것을 권장합니다.
3.  **알림 시스템 연동:** 새로운 랜딩 페이지 문의가 접수되었을 때 담당 MD에게 이메일이나 사내 메신저로 알림이 발송되도록 기능을 확장할 수 있습니다.

---
*본 보고서는 시스템의 안정성과 데이터 무결성을 보장하기 위한 KERYX 프로젝트 지침에 따라 작성되었습니다.*
