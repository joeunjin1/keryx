# KERYX 플랫폼 스킬 기반 업데이트 계획서

## 1. 개요
본 계획서는 새롭게 업로드된 KERYX 전용 스킬 및 공통 아키텍처 스킬들을 분석하여, 현재 KERYX 솔루션의 상태를 진단하고 스킬 가이드라인에 맞게 플랫폼을 고도화하기 위한 단계별 실행 계획입니다.

## 2. 신규 스킬 분석 및 현재 상태 진단

### 2.1. `keryx-platform-dev` (KERYX 전용 개발 가이드)
* **스킬 요구사항**:
  * 다중 포털(셀러, 공장, MD, 관리자)의 독립적 UI/UX 제공
  * 모든 페이지/컴포넌트의 완벽한 다국어(한국어/중국어) 지원 (`LangText` 사용)
  * 공통 레이아웃(`MobileLayout`) 적용 및 모바일 가독성 최우선
  * 최고 관리자(Admin Bypass) 권한 처리
* **현재 상태 진단**:
  * `MobileLayout` 및 `LangText` 컴포넌트는 존재하나, 전역 i18n 설정(`next-intl`)과 로컬 상태(`LangContext`)가 혼용되어 있어 일관성이 부족함.
  * `middleware.ts`에 관리자 바이패스 로직이 누락되어 있음.

### 2.2. `solution-architecture-foundation` (솔루션 제작 기본 헌법)
* **스킬 요구사항**:
  * 인라인 스타일 금지 (5가지 예외 케이스만 허용)
  * 하드코딩 금지 (디자인 토큰 사용)
  * 모바일 우선(Mobile-first) 디자인 적용
  * 반복 UI 컴포넌트화
* **현재 상태 진단**:
  * 이전 작업에서 인라인 스타일을 대폭 줄였으나, 여전히 일부 파일(예: `not-found.tsx`)에 하드코딩된 인라인 스타일이 남아있음.
  * `tailwind.config.ts`에 디자인 토큰이 정의되어 있으나, 코드 내 하드코딩된 색상값이 혼재함.

### 2.3. `living-sitemap-system` & `404-error-prevention-system`
* **스킬 요구사항**:
  * `sitemap.yaml`을 통한 단일 진실 공급원(Single Source of Truth) 유지
  * 라우트, API, DB 테이블, 권한, 다국어 키 등을 통합 관리
  * 빌드/CI 타임에 404 에러 및 누락 페이지 사전 차단
* **현재 상태 진단**:
  * `sitemap.yaml` 및 검증 스크립트가 부재함.
  * 라우트 및 메뉴 항목이 `src/config/navigation.ts`에 하드코딩되어 있어, DB 스키마나 실제 페이지 존재 여부와 동기화되지 않을 위험이 있음.

### 2.4. `sidebar-design-system`
* **스킬 요구사항**:
  * 포털별 단일 레이아웃(`layout.tsx`)에서 사이드바 관리
  * 메뉴 항목의 단일 출처 유지 및 권한 기반 렌더링
  * 모바일(Drawer) 및 데스크톱(고정) 완벽 지원
* **현재 상태 진단**:
  * `src/config/navigation.ts`를 통해 메뉴를 관리하고 있으나, 권한 기반 필터링 로직이 서버 사이드에서 완벽하게 강제되지 않음.

### 2.5. `trade-data-architecture` & `subscription-membership-system`
* **스킬 요구사항**:
  * 원가 데이터 격리, 공장/바이어 간 정보 차단 (RLS 정책)
  * 다중 승인 워크플로우 및 재고 무결성 강제
  * DB 기반 회원 등급(Tier) 관리 및 결제 연동
* **현재 상태 진단**:
  * Supabase RLS 정책에 대한 전면적인 점검이 필요함.
  * 코드 레벨의 권한 체크가 일부 존재하나, DB 레벨의 강제성이 부족할 수 있음.

## 3. 단계별 업데이트 계획

### 1단계: 아키텍처 기반 통합 및 정비 (Foundation & i18n)
* **목표**: 코드 헌법 준수 및 다국어 시스템 일원화
* **작업 내용**:
  1. `not-found.tsx` 등 잔여 인라인 스타일 및 하드코딩 값 제거 (Tailwind 및 디자인 토큰 적용).
  2. `next-intl`과 `LangContext`로 이원화된 다국어 시스템을 스킬 가이드(`LangText` 중심)에 맞게 통합 및 정리.
  3. `middleware.ts`에 최고 관리자(Admin Bypass) 접근 권한 로직 추가.

### 2단계: 네비게이션 및 사이트맵 시스템 구축 (Sitemap & Sidebar)
* **목표**: 404 에러 원천 차단 및 사이드바 일관성 확보
* **작업 내용**:
  1. `sitemap.yaml` 생성 및 현재 라우트/메뉴 구조 매핑.
  2. `check-sitemap.ts` 검증 스크립트 작성 및 CI/pre-commit 훅 연동.
  3. `src/config/navigation.ts`를 `sitemap.yaml` 기반으로 리팩토링하여 단일 진실 공급원 확립.
  4. 서버 사이드 권한 체크를 통한 사이드바 메뉴 렌더링 로직 강화.

### 3단계: 데이터 아키텍처 및 권한 강제 (Data & Security)
* **목표**: 무역 데이터 무결성 및 구독/회원 등급 DB 강제
* **작업 내용**:
  1. Supabase RLS 정책 전면 감사 및 스킬 원칙(원가 격리, 공장/바이어 차단)에 맞게 수정.
  2. 회원 등급(Tier)을 DB Enum으로 관리하도록 스키마 업데이트 및 관련 API 리팩토링.
  3. 주문/상품 승인 워크플로우 DB 트리거/RLS 적용.

### 4단계: UI/UX 고도화 및 모바일 최적화 (Mobile-first & Performance)
* **목표**: 모든 포털의 모바일 사용성 극대화 및 성능 향상
* **작업 내용**:
  1. `MobileLayout` 및 주요 페이지의 터치 타겟(최소 44px) 및 모바일 레이아웃 점검/수정.
  2. 이미지 최적화 파이프라인(WebP, Blurhash) 적용.
  3. 스켈레톤 로딩 및 에러 페이지 UX 개선.

## 4. 승인 요청
위 계획은 새롭게 추가된 스킬들의 핵심 원칙을 KERYX 플랫폼에 안전하고 체계적으로 적용하기 위한 로드맵입니다. 
대표님의 승인이 떨어지면 즉시 **1단계: 아키텍처 기반 통합 및 정비** 작업부터 착수하겠습니다.
