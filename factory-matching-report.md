# KERYX 공장 매칭 시스템 현황 및 개선 계획 보고서

## 1. 현재 시스템 현황 분석

현재 KERYX 솔루션의 공장 매칭 시스템은 바이어의 의뢰 접수부터 관리자의 상태 관리까지 기본적인 뼈대는 갖추고 있으나, 첨부된 HTML(keryx-factory-matching-form)과 같은 고도화된 다차원 평가 및 리포팅 워크플로우는 아직 구현되지 않은 상태입니다.

### 1.1. 데이터베이스 스키마 (`factory_matching_full.sql`)
- **`factory_matching_requests`**: 바이어의 의뢰 정보를 저장합니다. 제품 정보, 비즈니스 정보, 매칭 가중치(우선순위), 연락처, 관리자/MD 메모, 매칭된 공장 목록(`matched_factories` JSONB) 등을 포함합니다.
- **`factory_matching_factories`**: 매칭 후보 공장 정보를 저장합니다. 단가, MOQ, 리드타임, 인증, IP 감사 여부, MD 메모, AI 점수, 추천 여부, 바이어 공개 여부(`buyer_visible`) 등을 포함합니다.
- **`factory_matching_logs`**: 상태 변경 이력을 기록합니다.

### 1.2. 바이어 페이지 (`/apply/[type]/page.tsx`)
- 현재 바이어는 4단계의 간단한 폼(제품 정보, 비즈니스 정보, 가중치, 연락처)을 통해 의뢰를 접수합니다.
- 첨부된 HTML 폼처럼 다차원적인 질문(품질 기준, 리스크 분석, 세부 사진 분류 등)을 수집하는 구조가 아닙니다.

### 1.3. 관리자/MD 페이지 (`/admin/matching/page.tsx`)
- 칸반 보드 형태로 의뢰 상태(대기, 검토 중, 매칭 중, 샘플 진행, 완료, 취소)를 관리합니다.
- 의뢰 상세 정보와 메모를 확인하고 상태를 변경할 수 있지만, 공장 답변을 비교 분석하거나 최종 매칭 결론을 도출하는 구조화된 리포팅 기능은 부족합니다.

### 1.4. 공장 페이지
- 현재 공장이 직접 답변을 입력하거나 매칭에 참여하는 전용 인터페이스는 구현되어 있지 않습니다. MD가 공장 정보를 수집하여 `factory_matching_factories` 테이블에 직접 입력하는 구조로 보입니다.

---

## 2. 개선 목표 및 워크플로우 설계

바이어의 문제 제기부터 공장의 뒷받침 능력 확인, MD/관리자의 최종 해결 매칭까지 물 흐르듯 자연스럽게 이어지는 완전한 데이터 기반 매칭 워크플로우를 구축합니다.

### 2.1. 워크플로우 단계
1. **바이어 의뢰 (문제 및 목표 정의)**: 바이어가 고도화된 폼을 통해 제품 사양, 품질 기준, 리스크 요인, 다차원 가중치, 세부 사진 등을 상세히 입력합니다.
2. **공장 답변 (뒷받침 능력 확인)**: 후보 공장들이 바이어의 요구사항에 대한 구체적인 답변(단가, MOQ, 리드타임, 품질 보증 방안 등)과 증빙 자료를 제출합니다.
3. **MD/관리자 분석 및 결론 (최종 해결 매칭)**: MD와 관리자가 바이어의 가중치와 공장의 답변 데이터를 기반으로 다차원 평가(품질 매트릭스, 리스크 분석, 종합 점수)를 수행하고, 최적의 공장을 선정하여 바이어에게 리포트 형태로 제공합니다.

### 2.2. 주요 개선 사항
- **DB 스키마 확장**: 바이어의 상세 요구사항(품질 매트릭스, 리스크 요인 등)과 공장의 구체적인 답변 데이터를 저장할 수 있도록 스키마를 확장합니다.
- **바이어 매칭 의뢰 폼 고도화**: 첨부된 HTML 폼의 다차원 질문 및 사진 분류 기능을 `/apply/factory-matching` 페이지에 통합합니다.
- **공장 답변 페이지 신설**: 공장이 바이어의 의뢰 내용을 확인하고 구조화된 답변을 제출할 수 있는 전용 페이지를 구축합니다.
- **MD 매칭 결론 및 리포팅 페이지 구현**: MD가 데이터를 기반으로 공장을 평가하고 최종 결론을 도출하는 인터페이스와, 바이어에게 제공될 최종 매칭 리포트 페이지를 구현합니다.

---

## 3. 단계별 구현 계획

1. **DB 스키마 확장 및 바이어 매칭 의뢰 폼 구현**: `factory_matching_requests` 테이블 확장 및 `/apply/factory-matching` 페이지 고도화.
2. **공장 답변 페이지 및 MD 매칭 결론 페이지 구현**: 공장 전용 답변 폼 및 MD 평가/리포팅 인터페이스 개발.
3. **관리자 매칭 대시보드 고도화**: 칸반 보드에 다차원 평가 데이터 연동 및 리포트 생성 기능 추가.
4. **전체 통합 테스트 및 배포**: 바이어-공장-MD-관리자 간의 엔드투엔드 데이터 흐름 테스트 및 운영 환경 배포.
