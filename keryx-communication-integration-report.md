# KERYX 소통 시스템 완전 통합 분석 및 개선안

## 1. 현재 소통 시스템 파편화 현황

현재 KERYX 플랫폼에는 여러 번의 기능 추가로 인해 소통 관련 테이블과 페이지가 5개 이상으로 파편화되어 있습니다.

### 1.1. 파편화된 구 테이블 목록
- `service_requests` (10건): 공장매칭, 샘플개발, 시장조사 등 서비스 신청
- `service_request_replies`: 위 서비스 신청에 대한 MD 답변
- `unified_requests` (2건): 통합 의뢰
- `market_research_requests` (0건): 시장조사 의뢰 (미사용)
- `consultations` (3건): 랜딩페이지 무료 상담
- `request_reports`: 의뢰에 대한 결과 보고서

### 1.2. 신규 통합 테이블
- `conversations` (28건): 신규 통합 소통 시스템 (주로 일반 문의)
- `messages` (3건): 신규 통합 소통 시스템의 메시지

## 2. 통합의 필요성 및 문제점

1. **바이어 경험 저하**: 바이어는 "서비스 신청", "통합 의뢰", "MD 소통" 등 여러 메뉴를 돌아다녀야 자신의 문의 내역을 모두 확인할 수 있습니다.
2. **MD 업무 비효율**: MD 역시 여러 페이지를 확인해야 바이어의 요청을 놓치지 않을 수 있습니다.
3. **기능 중복 개발**: 파일 첨부, 실시간 알림, 자동 번역 등의 고급 기능을 모든 구 페이지에 각각 구현하는 것은 비효율적입니다.

## 3. 완전 통합 개선안 (신규 시스템으로 일원화)

모든 소통을 **`conversations` + `messages` 단일 구조**로 완전히 통합합니다.

### 3.1. 데이터 구조 통합 방안
구 테이블의 고유 기능(보고서 첨부, 진행 상태 타임라인 등)을 신규 시스템의 `metadata` JSONB 컬럼을 활용해 흡수합니다.

| 구 시스템 기능 | 신규 시스템 통합 방안 |
|---|---|
| 서비스 종류 (공장매칭/샘플 등) | `conversations.topic_type` 활용 (`factory_matching`, `sample_development` 등) |
| 입력 폼 데이터 (상품명, 수량 등) | `conversations.metadata` 내에 JSON 형태로 저장 |
| 진행 상태 (접수/진행중/완료) | `conversations.status` 확장 (`open`, `in_progress`, `closed` 등) |
| 결과 보고서 (PDF 등) | `messages.attachments` 활용 (MD가 보고서 파일을 메시지로 전송) |
| 랜딩페이지 상담 | 랜딩페이지 폼 제출 시 자동으로 `conversations` 생성 |

### 3.2. UI/UX 통합 방안 (Typeform 스타일 대화형 폼 적용)
- **바이어 포털**: 기존 `/seller/service-requests`, `/seller/requests`, `/seller/research` 메뉴를 모두 숨기고, 오직 **`/seller/md-chat` (MD 소통·의뢰)** 하나만 남깁니다.
- **의뢰 방식**: 바이어가 새 대화를 시작할 때 문의 유형(공장매칭, 샘플개발 등)을 선택하면, 챗봇이 질문을 던지는 방식(대화형 폼)으로 자연스럽게 정보를 입력받습니다.
- **MD 포털**: 기존 분산된 처리 페이지들을 숨기고 **`/md/communications`** 에서 모든 의뢰와 상담을 통합 관리합니다.

### 3.3. 추가 구현 핵심 기능 (1, 2순위)
1. **파일 첨부 기능 (1순위)**: `message-attachments` Storage 버킷을 활용하여 대화창 내에서 이미지, PDF, 엑셀 파일 등을 자유롭게 주고받을 수 있도록 구현합니다.
2. **Realtime 자동 갱신 (2순위)**: Supabase Realtime을 적용하여 새로고침 없이 새 메시지와 파일이 즉시 화면에 나타나도록 합니다.

## 4. 진행 순서 (제안)

1. **파일 첨부 기능 구현**: 신규 시스템에 파일 업로드 API 및 UI 추가
2. **Realtime 자동 갱신 구현**: 신규 시스템에 실시간 동기화 적용
3. **구 시스템 리다이렉트**: 기존 구 소통 페이지 접속 시 신규 통합 페이지로 자동 이동되도록 처리
4. **네비게이션 정리**: 사이드바 메뉴에서 구 소통 메뉴 완전 제거

---
위 개선안대로 진행하여 모든 소통 창구를 하나로 완벽하게 통합하겠습니다. 승인해 주시면 바로 1순위인 파일 첨부 기능부터 구현을 시작하겠습니다.
